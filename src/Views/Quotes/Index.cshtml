@model IEnumerable<TheMatrixAPI.Models.DTO.Quote.QuoteDTO>

@{
    ViewData["Title"] = "Quotes";
}

<div class="container">
    <h1 class="mt-4">Quotes</h1>
    <!--<form class="container" method="post" asp-controller="Quotes" asp-action="Index">
        <div class="row request-filter">
            <div class="col-7 d-flex flex-column justify-content-center">
                <div class="form-group d-flex align-items-center">
                    <label>Quote Line</label>
                    <select asp-for="AccountsId" asp-items="userAccountsItems" class="form-control"></select>
                    <span asp-validation-for="AccountsId" class="text-danger"></span>
                </div>
                <div class="form-group d-flex align-items-center">
                    <label>Transactions</label>
                    <select asp-for="TransactionTypeId" asp-items="transactionTypesItems" class="form-control"></select>
                </div>
            </div>
            <div class="col-3 d-flex flex-column"></div>
            <div class="col-2 d-flex flex-column justify-content-end">
                <input type="submit" class="btn btn-outline-light" />
            </div>
        </div>
    </form>-->
    <table class="table table-dark">
        <thead>
            <tr>
                <th>Quote Line</th>
                <th>Character Name</th>
                <th>Actor Name</th>
                <th width="160px"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var quote in Model)
            {
                <tr>
                    <td>@quote.QuoteLine</td>
                    <td>@quote.Character.Name</td>
                    <td>@quote.Character.ActorName</td>
                    <td class="quote_action-buttons">
                        <a asp-controller="Characters" asp-action="Edit" asp-route-id="@quote.Character.Id" class="btn btn-outline-warning"><i class="bi bi-pencil-square"></i></a>
                        <a asp-controller="Characters" asp-action="Edit" asp-route-id="@quote.Character.Id" class="btn btn-outline-danger"><i class="bi bi-x-circle"></i></a>
                    </td>
                </tr>
            }
            <tr>
                <td></td>
                <td></td>
                <td></td>
                <td class="quote_action-buttons">
                    <a asp-action="Add" class="btn btn-outline-success"><i class="bi bi-person-plus-fill"></i></a>
                </td>
            </tr>
        </tbody>
    </table>

</div>

<style>
    .quote_action-buttons {
        text-align: right;
    }
</style>